# Seedvale — Loose Ends

Krótkie, jednowierszowe notatki o pobocznych blokadach, pomysłach i wątkach
do dokończenia, które pojawiają się podczas implementacji planu, a nie
mieszczą się w jego zakresie. To nie jest issue tracker — gdy wpis dojrzeje
do realnej pracy, przenieś go do `docs/issues/` (nowy plik + wiersz w
`docs/issues/README.md`) i usuń stąd. Wpisy nieaktualne / już nieistotne też
usuwaj — lista ma zostać krótka.

Format: `- [ ] YYYY-MM-DD — opis (plan/plik, jeśli istotne)`

## Wpisy

- [ ] 2026-09-03 — `debug.household(id).history()` (plan settlements-npcs-013) nie widzi food-transferów z lokalnej wymiany (`NpcAgent.beginEconomyWithdraw`/`beginHouseholdExchange`, gałąź `kind === 'food'`): te ścieżki piszą bezpośrednio do `household.items`/`sourceHousehold.items` przez `depositFoodItems`/`claimFoodItems` (`items/foodItems.ts`), z pominięciem `Household.depositFood` — jedyny zinstrumentowany mutation point. Wood-exchange (ta sama funkcja, `kind !== 'food'`) idzie przez `Household.deposit` i jest widoczna. Świadomie pominięte w 013 (pierwszy slice = gathering/hunting/eating scenario, nie ma dotykać niepowiązanych systemów) — naprawa wymagałaby albo przepisania tych dwóch gałęzi na `depositFood`/`takeFood`, albo osobnego instrumentation point w `foodItems.ts`

- [ ] 2026-09-02 — `AnimalAgent.resolveNpcTarget()`'s village exclusion (npc-008 step 6 — a non-frenzied predator ignores NPC candidates inside `isNearVillage()`) only applies at acquisition (`senseNpcThreat()`). Once a predator locks onto an NPC (`npcTarget`), it keeps chasing/fleeing/ignoring that same NPC every tick without re-checking whether the NPC has since walked into a village — so a predator that started a chase outside a settlement can end up engaging inside one. Mirrors the existing, already-accepted "doesn't re-apply `playerNoticeRange` once locked" precedent in the same method; fixing it would mean re-validating the lock every tick, a small behaviour change beyond npc-008's "gate change + tuning" scope (implementation notes §5.6)
- [ ] 2026-09-02 — `cancelSourceTarget()` jest wołane w gałęziach player/fire/npc-scored, ale **nie** w `npc-attack-frenzied` ani `frenzy-beeline`: rozwścieczony wilk zachowuje `foodClaimedBy` na padlinie przez cały czas szarży na osadę. Świadomie nietknięte w refaktorze npc-008 (parytet zachowania), do rozstrzygnięcia osobno (recon planu npc-008, F2)
- [ ] 2026-09-02 — `AnimalAgent.update()` ma 16 argumentów pozycyjnych (`createFauna.ts` musi podawać `undefined` jako placeholder dla `onVocalize`) — dokładnie ta klasa ryzyka, która wywołała bug z `nearbyAnimalThreats` w `NpcAgent.update()` (`docs/research/2026-09-01-npc-animal-threat-forwarding.md`). Zamiana ogona sygnatury na obiekt opcji jest poza zakresem npc-008 (recon planu npc-008, F5)
- [ ] 2026-08-28 — plan fauna-003 (jazda konna) §4 chce, żeby dosiadalne zwierzę (koń/osioł) mogło podążać za graczem, gdy nie jest dosiadane, "korzystając z istniejącego follow/movement AI" — takiego mechanizmu (podążanie za graczem) nie ma nigdzie w `src/fauna`, jest tylko follow między zwierzętami (matka/stado, `herdCohesion.ts`). Świadomie pominięte przy implementacji: to nowa, nietrywialna AI (i koliduje z jawnym wykluczeniem taming/ownership z tego samego planu — bez więzi właściciel↔zwierzę nie ma podstawy, by nieoswojony koń wybrał podążanie akurat za graczem), nie mała rozszerzenie (`fauna-003-horse-riding-implementation-notes.md` §17 "Known limitations")
- [ ] 2026-08-27 — `waitForChunks(homeChunks())` w `buildWorldSystems` (`app/worldBundle.ts`) nadal czeka na cały przypięty blok 3×3 wokół domowej osady, mimo że `ChunkManager.readField()` ma już fallback (surowy szum) na czas nieukończonego czunka. Skrócenie tego oczekiwania (plan world-003 §2) zostało świadomie pominięte — ryzyko widocznej dziury/pop-inu dokładnie w miejscu spawnu gracza oraz szansa, że `settlementDefFor`'s site-search wybierze inne miejsce na podstawie fallbackowego szumu zamiast finalnego heightfieldu (z modyfikacjami terenu). Do rozważenia dopiero po zmierzeniu, czy to nadal dominujący koszt po odsprzężeniu `SettlementsManager`/fauny/preloadów (implementation notes `world-003-faster-application-startup-implementation-notes.md` §5)
- [ ] 2026-08-25 — house corner posts (`def.corners`, np. `corner_exterior_wood`) nie mają własnego collidera — `buildHouseWallCollidersLocal` (`settlement/houseBuilder.ts`) iteruje tylko `def.walls`. Zwykle maskowane przez zasięg sąsiedniego wall modułu (0.95 + entity radius), ale to przypadkowe, nie invariant — zawodzi, gdy sąsiedni moduł jest skipniętym door module bez fallbackowego wall collidera (plan "House collider alignment and scale cleanup", implementation notes §2026-08-25)
- [ ] 2026-08-24 — usunięcie player-built grządki (plan 176, `care` <= 0) nie kasuje kaskadowo cropów zasadzonych na jej terenie (`plantedCrops`/`ChunkManager`) — świadome ograniczenie zakresu (implementation notes plan 176 §6 sugerowały kaskadę jako "cleanest rule", ale wymagałoby to nowej `ChunkManager` operacji "usuń cropy w promieniu", poza zakresem M-effortu). Osierocony crop po prostu przestaje mieć właściciela: `findNearestGarden` nic nie znajdzie, więc nadal rośnie/dojrzewa z pełną produktywnością (bez kary za zaniedbanie, bo nie ma już czego zaniedbywać) (`2026-08-20--176--garden-and-field-maintenance.md`)

- [ ] 2026-08-21 — `Settlement` nie eksponuje `GardenScale` per `landmarks.gardens[i]` (tylko pozycje), więc walidacja sadzenia crop-a (`world/plantedCrops.ts`'s `isNearAnyGarden`) używa jednego konserwatywnego promienia (`gardenClearingRadius('L')`) zamiast dokładnego footprintu S/M/L danego ogródka — S/M ogródek dopuszcza sadzenie nieco dalej niż jego realne grządki. Wymagałoby przeniesienia `gardenScale`/`plan.landmarks` na `Settlement`/`SettlementLandmarks` (plan `2026-08-16--126--seed-planting.md`)

- [x] 2026-08-16 — `NpcAgent.getCurrentActivity()` pokazuje ore-deliver leg (`deposit` po `mine`) jako `need: idle` zamiast `work`, bo chain traci informację o rodzicu po promocji `next` → `pendingAction`; kosmetyczny błąd etykiety dialogowej, bez wpływu na gathering/ekonomię (plan `2026-08-16--131--natural-resource-gathering.md`) — naprawione 2026-08-18: `NpcPlannedAction.chainKind` przenosi klasyfikację rodzica przez promocję `next` (`NpcAgent.ts`'s `promoteChainKind`/`classifyPendingActivity`, testy w `src/ai/npcCurrentActivity.test.ts`); wood chop→deposit (need-driven) bez zmiany zachowania
- [x] 2026-08-16 — spawn-point lifecycle (`PreySpawner.state`/`deathsThisCycle`/`disabledAtDay`) nie jest persystowany; reload po `[E] Zniszcz` odradza wypaloną spawn point z powrotem jako `active` (dead reckoning z seeda, tak jak przed planem 125). Zamierzone ograniczenie zakresu L-effortu (plan nie testuje tego w kryteriach akceptacji) — jeśli konsekwencja ma przetrwać reload, potrzebna jest zwarta kolekcja w `SaveData` keyed by `PreySpawner.id` (plan `2026-08-16--125--fauna-spawn-point-population-limits.md`, patrz implementation notes §Persistence) — naprawione 2026-08-18: `SaveData` v17 `spawnPoints` (`persistence/saveData.ts`), `AnimalSpawner.ts`'s `snapshotSpawnPointState`/`restoreSpawnPointState`, wired przez `createFauna`/`worldBundle.ts` (save/load **i** in-session `rebuildWorldBundle`); `disabled`/`recovering` restore nie odtwarza zwierząt i ponownie przypala prop/teren; brak testu w przeglądarce
- [ ] 2026-08-19 — jaskinia jest siedliskiem drapieżników (`cave` → `wolf`); niedźwiedź ma tam trafić, gdy gatunek powstanie — nie dodawać osobnego spawn pointu, tylko `SPAWNER_SPECS` / `kind` (plan 125 follow-up)
- [ ] 2026-08-17 — vegetation to **311 `InstancedMesh`ów na 709 instancji** (2,3 instancji na draw call): `buildInstancedProps` batchuje per `(chunk, species, primitive)`, więc instancing prawie nic nie daje. Merge cross-chunk to największa dostępna redukcja draw calls i dotyczy każdego passa — ale to realny refactor `render/instancedProps.ts` + `terrain/chunkManager.ts` (research `2026-08-17--019--rendering-optimizations.md` §4.3)
- [ ] 2026-08-17 — `N8AOPass` renderuje scenę **drugi raz** (własny depth/normal override) wewnątrz `RENDER`. `N8AOPostPass` (reuse depth texture) to alternatywa, ale zmienia architekturę renderu i dotyka podejrzanego o `GL_INVALID_OPERATION` MRT handlingu n8ao vs three@0.185 (research `2026-08-17--019--rendering-optimizations.md` §4.2, `2026-08-17--015--streaming-hitch-gl-errors-handoff.md`)
- [x] 2026-08-17 — repo ma dwa lockfile'e: `packageManager: pnpm@11.20.0` + `pnpm-lock.yaml`, ale CI robi `npm ci` na `package-lock.json`. Rozjazd już raz wywalił CI na `main` (n8ao 2.0.0 vs 2.0.1); zresynchronizowane, ale rozjedzie się znowu — wybrać jeden menedżer (research `2026-08-17--017--threejs-rendering-audit.md` §4 F8)
- [ ] 2026-08-17 — melee ma dwa niezależne pojęcia „kierunku ataku”: `player.faceToward()` obraca mesh (i robi to jeszcze **przed** `gapClose()`, więc po kolizyjnym zsunięciu mesh patrzy lekko obok), a hit test (`resolveMeleeHits`) liczy się z yaw kamery. Plan 142 pogodził je tylko dla touch (`attackYaw`); desktop nadal może wizualnie patrzeć w cel i chybić, jeśli kamera odjedzie w trakcie wind-upu (plan `2026-08-17--142--mobile-combat-target-acquisition.md`)
- [ ] 2026-08-17 — pułapka podniesiona (`[R] Zabierz`) wraca do ekwipunku z pełną trwałością: `Inventory` jest count-based, więc nie ma gdzie zapisać `remainingDurability` przedmiotu. Drobny exploit (zdjąć na 0.1 trwałości → postawić jak nową); wymagałby per-instance item state, co jest poza zakresem planu 141 (plan `2026-08-17--141--animal-traps.md`)
- [ ] 2026-08-17 — pułapki łapią tylko w zasięgu aktywnej symulacji fauny (`Fauna.getAgents()`); zwierzęta nie są symulowane poza załadowanym obszarem, więc pułapka zostawiona daleko od gracza nie „doliczy” złapań wstecz. Zamierzone ograniczenie zakresu planu 141 §19 — pełny off-screen trapping wymaga symulacji fauny dla nieaktywnych chunków (plan `2026-08-17--141--animal-traps.md`)
- [ ] 2026-08-19 — plan 149 Phase 1 A nie prewarmuje MeshDepthMaterial / EffectComposer (~26–27 programów na frame 0). Pierwszy draw już zlinkowanych programów sceny nadal kosztuje ~180–420 ms (`onFirstUse`) za overlayem loadera. Nie dokładać drugiej warstwy `compileAsync` bez osobnego małego planu (`2026-08-17--149--shader-program-first-use-hitch.md` §19)
- [ ] 2026-08-21 — Etap 2 kandydaci po podziale `createApp.ts` (Etap 1): `src/app/gameLoop.ts` (~1640 linii — tick simulation + interakcje `[E]`/`[R]` + melee/ranged rozstrzyganie + day/night/weather apply + render/minimapa w jednym pliku) i `src/app/interactables.ts` (~720 linii — jeden `buildInteractables` obsługujący ~20 rodzajów kandydatów). Oba to osobna, świadoma robota, nie doklejka do Etapu 1
- [x] 2026-08-21 — "Wyrzuć" dla instance-backed kinds (siekiera/nóż/sierp/pułapki — `isInstanceBackedKind`) teraz działa (`inventoryWiring.ts`'s `dropItemStack`, `InventoryScreenItemList.vue`/`InventoryScreenItemDetails.vue`), ale świadomie tylko jako zwykły item: `bundle.droppedItems.drop(kind, x, z)` nie niesie `durability`/`sharpness` instancji, więc wyrzucony i ponownie podniesiony nóż wraca ze świeżą, domyślną kondycją zamiast tej, jaką faktycznie miał. Wymaga instance-aware world-drop (props na dropniętym itemie + ich odczyt przy podniesieniu), zanim to przestanie być stratą informacji — **potwierdzone i rozszerzone przez audyt planu 194**: dokładnie 4 miejsca tracą instance state (`dropItemStack`, pickup w `gameLoop.ts:1219-1223`, `grantItem`'s overflow fallback, plus osobny hotkey `consumeDrop()` który *milcząco pomija* instance-backed kinds zamiast tracić dane — druga, rozbieżna implementacja "wyrzuć wszystko" wymagająca tej samej poprawki co `dropItemStack`); wzorcem poprawnego rozwiązania jest `PlacedTraps.place()`/`.collect()`, które już poprawnie przenoszą `ItemInstance.id`/`durability`. **Naprawione 2026-08-22** (plan `2026-08-22--199--arch--entity-identity-transfer-continuity.md`): `DroppedItem`/`SaveDroppedItem` gained an optional `instance?: SaveItemInstance` field (mirrors `PlacedContainerRecord`'s shape); `createDroppedItems.ts`'s `drop()`/`collect()` carry it through unchanged. All 4 call sites now instance-aware — `dropItemStack` and the `consumeDrop()` hotkey both drop the real `ItemInstance` (new shared `toSaveItemInstance()` helper on `Inventory.ts`), pickup restores it via `Inventory.instancesFromJSON([...])` instead of minting a fresh default (`createAcquiredInstance` only as a fallback for non-instance-backed pickups), and `grantItem`'s overflow-drop carries the instance it had already minted.
- [ ] 2026-08-21 — plan 181 Etap 4–6 (sieć rzeczna jako tile'e 256m + halo, chunk-continuous ribbon geometry, lekki water material) i Etap 7 (meandrowanie, width/flow shading, **wodospady** — dodane 2026-08-25 jako per-vertex `aFall` na istniejącym ribbonie, `waterfallFactor()` w `riverGeometry.ts`, bez nowej geometrii/systemu) zaimplementowane — plan 181 domknięty (`verification needed`). Świadomie nadal odłożone, bez uzasadnienia pomiarem/potrzebą: pełna parytetowość shadera z jeziorem/oceanem (reflection binding, depth-based foam — river material już współdzieli paletę dzień/noc i `tickWaterTime`/`setWaterDayNight`), worker offload dla obliczeń tile'a hydrologii (main-thread, ~18ms/tile zmierzone syntetycznie; ten sam warunek "profile before deciding" powtórzony w planie 189), oraz interaktywny debug-overlay do wizualnej inspekcji hydrologii w przeglądarce (cel diagnostyczny planu §3 już spełniony przez `hydrology.test.ts`'s multi-seed evaluation, a rzeki są teraz realnie renderowane i widoczne w grze) (`2026-08-21--181--natural-mountains-and-rivers.md`, patrz "Etap 7 completion" w treści planu)
- [x] 2026-08-22 — Stan runtime NPC (w tym śmierć) nie przetrwa zwykłego strumieniowania osady — tylko save/load. Każda nie-domowa osada przy oddaleniu/przybliżeniu do `unloadRadius`/`loadRadius` (420m/300m) w pełni disposuje i odtwarza `NpcAgent`y (`SettlementsManager.ts:378-400`, `createSettlement.ts:510-540`) — HP/needs/vigor/stamina/phase/inventory wracają do świeżych wartości, w tym **zabity NPC ożywa** przy następnych odwiedzinach tej samej sesji, bez potrzeby reload (`NpcAgent.ts:930-947` konstruuje stan bezwarunkowo; brak registry analogicznego do `HouseholdRegistry`/`EconomyRegistry`, które celowo przetrwają ten sam cykl). Dodatkowo: martwy NPC nie ma żadnej ścieżki usunięcia/disposal (w przeciwieństwie do fauny: `fresh→rotting→bones→removed`). **Naprawione 2026-08-22** (plan `2026-08-22--197--arch--npc-runtime-state-lifecycle-continuity.md`): nowy `NpcStateRegistry` na `SettlementsManager` (`src/settlement/npcState.ts`, mirror `HouseholdRegistry`/`EconomyRegistry`) trzyma `health`/`stamina`/`vigor`/`needs` keyed po stabilnym `npc.id`; `NpcAgent`'s konstruktor hydratuje z tego samego obiektu zamiast tworzyć świeży (bezpośrednia referencja, nie kopia), i wywołuje `die()` od razu przy hydration jeśli `health.dead` — martwy NPC nie "ożywa" po dispose/recreate. Przetrwa też `WorldBundle` rebuild (`snapshotNpcStates()`/`initialNpcStates`, mirror `carriedEconomies`) — nie jest w `SaveData` (świadomie, poza zakresem 197). Usunięcie/disposal pozostaje nie zaimplementowane (martwy NPC zostaje w `settlement.npcs` na stałe, jako trwały "corpse in place") — świadomie poza zakresem 197 (brak systemu corpse/loot dla NPC, jak dla fauny), ale wykluczony z O(n²) separation loop (`createSettlement.ts`) i z dialogue targeting (`interactables.ts`) tak jak combat już go wykluczał.
- [x] 2026-08-22 — Identity questodawcy/relacji (`QuestDef.giverName`, `QuestManager.relations`) to goły string imienia, globalnie, bez powiązania z `NpcAgent.id` ani osadą. 4 zarezerwowane imiona questodawców (`Anna`/`Piotr`/`Kasia`/`Marek`, `characters.ts:41-51`) pochodzą z tej samej puli kulturowej (`nameCultures.ts`), z której `generateNpcName()` losuje **każdego** proceduralnego NPC na świecie, bez wykluczenia — dla pechowego seeda inny, niepowiązany NPC może dostać etykietę/dialog questodawcy i dzielić jego relation score. Naprawa (tania): wykluczyć 4 zarezerwowane imiona z puli `generateNpcName` per kultura (plan `2026-08-22--194--arch--entity-identity-lifecycle.md`, P1). **Naprawione 2026-08-22** (plan `2026-08-22--199--arch--entity-identity-transfer-continuity.md`): `nameCultures.ts`'s `generateNpcName()` filters `RESERVED_CHARACTERS`' 4 names out of the culture pool before rolling — the reserved characters themselves are still assigned directly in `families.ts`'s `reservedHomeFamilies()`, never through `generateNpcName`, so this only removes the collision risk. Kept the existing name-based identity (not a full `NpcId` migration of `QuestDef`/save-data relations) — uniqueness was the actual bug, and the reserved 4 are always in the never-streamed home settlement, so the "same id → same live NPC" continuity plan 197 already guarantees is enough once names can't collide.
- [x] 2026-08-22 — Mid-session `rebuildWorldBundle()` (np. World Config screen, ten sam handler co zmiana ustawień bez zmiany seeda) nie unieważnia `QuestManager.animalTargets` dla dzikiej fauny — ta walidacja istnieje tylko w konstruktorze (ścieżka save/load, `QuestManager.ts:159-176`), nie w `rebuildWorld()` (`createApp.ts:707-722`, `questManager.reset()` jest gatowane `resetCollectedItems`, które ta ścieżka domyślnie ma `false`). Ponieważ `nextAnimalId` resetuje się do 0 per gatunek przy każdym `createFauna()` (`createFauna.ts:506,532`), rebuild na tym samym seedzie może po cichu przypisać istniejące id questowe nowemu, niepowiązanemu zwierzęciu (błędne dowiązanie) albo trwale soft-lockować quest. Naprawa: wywołać tę samą inwalidację co konstruktor również z `rebuildWorld()`/`rebuildWorldBundle()` (plan `2026-08-22--194--arch--entity-identity-lifecycle.md`, P1). **Naprawione 2026-08-22** (plan `2026-08-22--199--arch--entity-identity-transfer-continuity.md`): new `QuestManager.invalidateStaleAnimalTargets()` mirrors the constructor's save/load-restore handling — wild-fauna-kind bindings go `invalidated` (terminal, matches the existing "world binding can't be trusted" semantics), livestock-kind bindings are cleared and rebound via the live resolver. Called from `createApp.ts`'s `rebuildWorld()` whenever `resetCollectedItems` is `false` (the `true`/new-world path already clears `animalTargets` via `questManager.reset()`).
- [x] 2026-08-22 — `ResourceDeposits` (złoża rudy) nie mają żadnego trwałego stanu wydobycia: `remaining` żyje wyłącznie na żywej `DepositInstance` i resetuje się do pełna zarówno przy zwykłym oddaleniu/powrocie w ramach własnego streamingu radius (`despawn()`+`spawnSync()`, `resourceDeposits.ts:183-248` — **farming exploit osiągalny zwykłą grą**, P1), jak i przy każdym `rebuildWorldBundle()` (`worldBundle.ts:540,570`, brak carry-snapshot, w przeciwieństwie do `treeOverrides`/`carriedEconomies` — potwierdza wcześniej niepewny finding planu 193; konkretny, niezabezpieczony trigger: World Config screen's flat-shading toggle, `WorldConfigScreen.vue:18-19`, bez confirm dialogu, P2). `docs/architecture/ARCHITECTURE.md` nie dokumentuje braku carry dla `ResourceDeposits`. **Naprawione 2026-08-22** (plan `2026-08-22--198--arch--world-resource-state-continuity.md`): nowy `ResourceDepletionState` (`Map<id, remaining>`, `terrain/depositMining.ts`'s `resolveRemaining`/`recordMined`/`isDepleted` — brak wpisu = deterministyczny initial z `hitsForRichness`, `0` = wyczerpane) zastępuje sesyjny `depletedIds` w `resourceDeposits.ts`; `mine()` zapisuje w tym samym miejscu, w którym zmienia `remaining` (jedno źródło prawdy dla playera i NPC, bo oba idą przez ten sam `ResourceDeposits.mine()`). Owner: `createApp.ts` (jak `collectedItemIds`), przewleczony przez `createWorldBundle`/`rebuildWorldBundle` → `buildResourceDeposits`; resetowany do świeżej `Map` tylko przy `resetCollectedItems` (genuinely new world). Świadomie poza `SaveData` (in-session continuity, plan §8) — pełna persistence między sesjami zostaje osobnym follow-upem; plan `2026-08-22--200--arch--persistence-gaps-authoritative-state.md` scoped resource-deposit continuity out explicitly ("poza zakresem: resource deposit continuity — 198"), więc follow-up przenosi się do `2026-08-22--201--arch--deferred-architecture-state-cleanup.md`. **Domknięte 2026-08-22** (plan 201): `SaveData.resourceDeposits` (sparse `Record<id, remaining>`, mirror `spawnPoints`/`harvestedCropIds`) — `createApp.ts` seeduje `resourceDepletion` z `initialSave?.resourceDeposits` przy starcie, `saveState.ts`'s `buildSaveData` serializuje `Object.fromEntries(getResourceDepletion())`; reset do pustej `Map` dzieje się tylko na `resetCollectedItems` (nowy świat), jak reszta tej rodziny stanu.
- [ ] 2026-08-21 — `src/items/trade.ts`'s `wouldFitAfter()`/`wouldFitAfterInstance()` (używane przez `buyWithCoins`/`buyWithBarter`) sprawdzają tylko wagę, nie gabarite (`maxSize`, plan 164). Dla stackable kind (nieinstancyjny branch `buyWithBarter`/`buyWithCoins`) wynik `inventory.add(kind, 1)` jest też ignorowany — jeśli tylko limit gabarite (nie waga) zostałby przekroczony, transakcja i tak zwróci `'ok'` (monety/oferta znikają, przedmiot faktycznie nie trafia do ekwipunku, bo `Inventory.add()` wewnętrznie i tak odrzuca przez `canAdd()`). Nieodtworzone w praktyce, znalezione przy okazji naprawy `isValidOffer` dla instance-backed barter offers (patrz `git log` `src/items/trade.ts`, 2026-08-21) — wymaga sprawdzenia `canAdd`/dodania gabarite do `wouldFitAfter*` + użycia zwracanej wartości `add()`/`addInstance()` w stackable branchu
- [x] 2026-08-22 — podczas `timeSkip` NPC needs/vigor/stamina są liczone **dwa razy**: `worldDt` (przyspieszone `dayNight.timeMultiplier`, np. ×20 przy `dayLengthSec=480`) trafia co klatkę do żywego `NpcAgent.update()`/`tickNeeds()` (`gameLoop.ts`'s `bundle.settlementsManager.update(worldDt, ...)`, bez żadnego gate'u na `timeSkip.isActive()`), a po zakończeniu skipu `NpcAgent.resolveTimeSkip()` odtwarza te same godziny jeszcze raz jako osobny catch-up. Komentarze w `world/timeSkip.ts`/`gameLoop.ts` opisują NPC/faunę jako „frozen" podczas skipu — w kodzie nic tego nie egzekwuje (`NpcAgent.frozen` istnieje tylko jako debug-inspector toggle). To samo dotyczy NPC melee/ranged (`combat/meleeAttack.ts`/`rangedLifecycle.ts`) — NPC-side swing/draw timing przyspiesza razem z `worldDt`, player-side zawsze na surowym `dt`. Poza zakresem planu 192 (ten plan miał zachować obecne zachowanie time-skip, nie naprawiać je) — wymaga osobnego planu: albo faktyczne zamrożenie `settlementsManager.update`/`fauna.update` podczas `timeSkip.isActive()` (przywracając udokumentowany model), albo świadome usunięcie `resolveTimeSkip`'s catch-up jako zbędnego (`2026-08-22--192--arch--time-and-simulation-consistency.md`) — **rozszerzone przez audit planu 193** (`2026-08-22--193--arch--simulation-architecture-consistency.md`): (1) `resolveTimeSkip`'s replay realnie duplikuje ilości surowców w `Household`/`SettlementEconomy` (`household.water.add(...)`, drewno/jedzenie — `NpcAgent.ts:2126-2136`), nie tylko paski potrzeb — silniejszy bug niż sądzono; (2) replay wody jest bezwarunkowy (`NpcAgent.ts:2128-2129`), pomijając `household?.water.has(...)` gate, który żywa ścieżka egzekwuje — time-skipowany NPC nigdy nie może być spragniony przy pustej beczce; (3) **fauna ma ten sam brak gate'u co NPC, ale bez żadnego catch-up** — `bundle.fauna.update(worldDt, ...)` (`gameLoop.ts:1601-1653`) tika pełne drzewo zachowań (ruch, walka drapieżnik-ofiara, obrażenia gracza/NPC) do ~20× przyspieszone i w pełni widoczne dla symulacji, sprzecznie z jawnym komentarzem-inwariantem w `world/timeSkip.ts:48-52` ("would fly off into the void... world keeps simulating at its normal real-time pace"); wilk może w pełni upolować/zabić inwentarz żywy lub NPC, albo trafić gracza, niewidocznie podczas 8-godzinnego snu — P0, nowe, najpoważniejszy finding audytu 193. Rekomendacja: jeden follow-up plan (`arch--timeskip-simulation-gating`) rozstrzygający model (prawdziwe zamrożenie+catch-up dla NPC/fauna/economy, albo świadomy live-tick z usuniętym redundantnym replay + poprawionymi komentarzami) — **audyt planu 194 doprecyzował konsekwencję dla fauny**: martwa gałąź `AnimalAgent.update()`'a (`timeSinceDeath`) tika na tym samym zawyżonym `worldDt` co ruch/walka (`AnimalAgent.ts:1307-1313`) — świeżo powstałe zwłoki mogą w pełni przejść `fresh→rotting→bones→disposeAgent()` (trwałe usunięcie mesh/GLB) w ciągu kilku realnych sekund od startu 8-godzinnego skipu, zanim nawet zniknie overlay; ten sam root cause, silniejsza (disposal, nie tylko wizualna) konsekwencja — **naprawione 2026-08-22** (plan `2026-08-22--196--arch--time-skip-simulation-semantics.md`): `gameLoop.ts`'s `tick()` teraz gate'uje `settlementsManager.update`/`fauna.update`/`placedTraps.update` za `!timeSkip.isActive()` (zero live tick podczas skipu, żadnego `worldDt` scalowania), więc `NpcAgent.resolveTimeSkip`'s replay jest jedynym mutatorem danego okresu (double-count usunięty, wraz z bezwarunkowym replay wody — nieaktualny teraz, że live tick w ogóle nie biegnie). Nowy `AnimalAgent.resolveTimeSkip()`/`Fauna.resolveTimeSkip()` daje faunie analogiczny, minimalny (nie pełny off-screen sim) deterministic catch-up: `timeSinceDeath` dla zwłok (istniejący per-frame `advanceCorpseDecay`/`readyToRemove()` sam się koryguje na następnej normalnej klatce) i jeden zbiorczy `tickAnimalLife()` dla głodu/pragnienia/staminy żywych zwierząt — bez ruchu/walki/AI. Oba stare, sprzeczne komentarze (`gameLoop.ts`, `world/timeSkip.ts`) przepisane na zgodne z rzeczywistym kodem
- [x] 2026-08-22 — `Household.stock`/`.water` (`HouseholdRegistry` na `SettlementsManager`) nie ma carry-mechanizmu przez in-session rebuild `WorldBundle` (np. zmiana ustawienia terenu przez World Config screen / debug GUI wywołuje `rebuildWorld()` → `rebuildWorldBundle`), w przeciwieństwie do `SettlementEconomy` (`EconomyRegistry`), który jest jawnie przenoszony (`carriedEconomies`, `worldBundle.ts:538`). Efekt: rodzina z prawie pustą lub prawie pełną spiżarnią po takim rebuildzie cicho wraca do świeżych, jitterowanych wartości startowych, mimo że stock osady (który mógł już odzwierciedlać ten sam niedobór) zostaje zachowany. Nie dotyczy zapisu gry (`Household` i tak nie jest w `SaveData`) — tylko żywej sesji. **Naprawione 2026-08-22** (plan `2026-08-22--197--arch--npc-runtime-state-lifecycle-continuity.md`, §8): `Household.snapshot()`/`HouseholdRegistry.serialize()` + `initial?` param na `createHousehold`/`createHouseholdRegistry`, dokładny mirror `SettlementEconomy`/`EconomyRegistry`; `carriedHouseholds` przewleczone przez `rebuildWorldBundle` tym samym torem co `carriedEconomies` (plan `2026-08-22--195--arch--data-state-consistency.md`, finding B)
- [x] 2026-08-22 — `PlayerNeeds.starvationDuration`/`.dehydrationDuration` (`src/player/PlayerNeeds.ts`, plan 165 — simulation-time liczniki „jak długo hunger/thirst siedzi krytycznie nisko", gate'ujące realny HP-loss w `playerDamage.ts`) nie są persystowane w `SavePlayerNeeds` (który ma tylko `hunger`/`thirst`/`vigor.current`), mimo że `hunger.current`/`thirst.current` — z którymi te liczniki są skorelowane — są. Efekt: zapis podczas trwającego starvation/dehydration HP-drain i reload zatrzymuje drenaż HP na cały grace-period (`hungerSevereDurationSec`) mimo że głód/pragnienie nadal jest krytyczne — cichy save-scum wokół śmierci z głodu/pragnienia (plan `2026-08-22--195--arch--data-state-consistency.md`, persistence cluster finding P1). **Naprawione 2026-08-22** (plan `2026-08-22--200--arch--persistence-gaps-authoritative-state.md`): `SavePlayerNeeds` gains both fields (save v27, `SavePlayerNeedsV27`), `saveState.ts`/`restorePersistedNeeds` round-trip them; pre-v27 saves migrate with both defaulted to `0` (`toV27`, same as a fresh `createPlayerNeeds()`).
- [x] 2026-08-22 — `NpcAgent.beginOreGathering` (`src/ai/NpcAgent.ts:2527`) gate'uje start wydobycia na `this.carried.canAdd(itemKind, 1)` (miejsce na **1** jednostkę), ale faktyczny yield (`result.yield.count`) może być >1, i paruje się z późniejszym `this.carried.remove(itemKind, minedCount)` bez sprawdzenia, czy `add()` w ogóle się powiódł (plan `2026-08-22--195--arch--data-state-consistency.md`, player/items cluster finding). **Naprawione 2026-08-22** (plan `2026-08-22--201--arch--deferred-architecture-state-cleanup.md`): `mine`'s `onComplete` teraz ustawia `minedCount` tylko gdy `this.carried.add(...)` faktycznie zwróci `true`, więc `deposit`'s `remove(itemKind, minedCount)` nigdy nie zdejmuje jednostek, które nie zostały dodane.
- [x] 2026-08-22 — `NpcAgent.die()` (`src/ai/NpcAgent.ts:1669-1685`) nie ma żadnego death-propagation hooka, w przeciwieństwie do livestock's `onAnimalDeath` (`createSettlement.ts:209-211`): zatrzymuje mesh/animację i zapisuje trace event, ale nic nie informuje `Household` (liczebność rodziny), `SettlementEconomy` ani `QuestManager` (jeśli ten NPC był questodawcą), że NPC zginął. **Zamknięte 2026-08-22** (plan `2026-08-22--197--arch--npc-runtime-state-lifecycle-continuity.md`, §6) bez dodawania hooka: `Household`/`SettlementEconomy` amounts są running balances mutowanymi przez żywe akcje NPC, nie wyprowadzane z liczby żywych członków — martwy NPC (którego `update()` już wcześniej no-opuje na `health.dead`) po prostu przestaje kontrybuować/konsumować, więc nic nie "gnije" bez propagacji; questodawca-identity zostaje w planie 199 (poza zakresem 197). Jedyna realna, zademonstrowana konsekwencja znaleziona przy audycie — dialog gracza z martwym NPC wciąż się otwierał (`interactables.ts`'s gaze-lista nie filtrowała `health.dead`) — naprawiona wprost, filtrem jak przy combat-targeting (`playerCombat.ts`). Jeśli przyszła funkcja (np. reputacja/village population UI) faktycznie potrzebuje reagować na śmierć NPC, dodać wąski `onNpcDeath?(npcId, settlementId)` hook wtedy, mirror `onAnimalDeath`.
- [x] 2026-08-22 — `src/simulation/`'s `DecisionContext.extras`/`.entity` były write-only/martwe w obu jedynych konsumentach: NPC odczytywał z `decisionContext` tylko `scheduleActivity` (wartość, która i tak trafia jako parametr — `NpcAgent.ts:1818,1830,2276-2295`), fauna nigdy nie czytała `ctx.extras` (`AnimalAgent.ts:1472-1519`); `.entity` nie był ustawiany przez żadnego z dwóch konsumentów. Bez wpływu na poprawność, ale myląca sygnalizacja architektoniczna (plan `2026-08-22--193--arch--simulation-architecture-consistency.md`, finding P2 #4). **Przycięte 2026-08-22** (plan `2026-08-22--201--arch--deferred-architecture-state-cleanup.md`): `DecisionContext.entity`/`.extras` usunięte z `src/simulation/types.ts` razem z ich write-only `extras: {...}` blokami w `NpcAgent.buildDecisionContext`/`AnimalAgent.buildDecisionContext`; `SimulationEntityRef` sam typ zostaje (realnie używany jako `CombatTargetHandle.ref` w `combat/combatIntent.ts`). Osobna, niezwiązana część tego samego findingu (P2 #5 — NPC pomija shared `adoptPlannedAction`, `NpcAgent.startAction()`'s `NpcAgent.ts:2234-2256` reimplementuje "always replace" zamiast fauna's `adoptPlannedAction`, `AnimalAgent.ts:1468`) **pozostaje otwarta** — to zmiana zachowania (adopt-same-kind vs always-replace), nie czysty trim, więc wymaga świadomej decyzji, nie mieści się w zakresie 201 jako "mały fix"; podjąć przy najbliższej okazji dotykania action-lifecycle NPC/fauna.
- [ ] 2026-08-24 — szerszy temat modyfikacji terenu wystartował (plan `world-terrain-002--terrain-modification-and-land-preparation.md`): `Wyrównaj`/`Zrób górkę` (lokalna deformacja łopatą) + `Przygotuj teren` (większy, world-space job z preview/pracą/persistencją aktywnego stanu). Zamierzona baza pod przyszłą konstrukcję/grządki/pola/budynki/drogi — patrz `docs/vision/world.md`'s "Terrain modification as a foundation". Żaden z tych przyszłych konsumentów nie jest tym planem zaimplementowany.
- [ ] 2026-08-24 — teren skalisty/górski (`isRockGround`) dla `Przygotuj teren` wymaga dziś tylko obu narzędzi (łopata+kilof) bez żadnej kary za trudność — plan `world-terrain-002` §3 świadomie odłożył "przyszłą karę terrain-difficulty" poza zakres v1. Docelowo powinna istnieć jawna kara (np. wolniejsza praca / wyższy `requiredWork`) dla przygotowania terenu skalistego, analogicznie do istniejącego zróżnicowania soil/sand/rock w `terrain/dig.ts`.
- [x] 2026-08-24 — `ChunkManager`'s `modifications` (dig/level/scorch/prepare — `terrain/chunkManager.ts`) nigdy nie były częścią `SaveData` (świadomy, przed-istniejący zakres planu 052 — potwierdzone w kodzie przy okazji planu `world-terrain-002`): każdy ślad kopania/wyrównywania/wypalenia/**ukończonego** `Przygotuj teren` znikał po pełnym save/load (app restart), mimo że przetrwał zwykłe stream-out/in chunków w tej samej sesji. **Naprawione 2026-08-24** (plan `world-terrain-save`): `modifications` stało się caller-owned polem `ChunkManagerConfig` (ten sam wzorzec co `plantedTrees`/`plantedCrops`), co jednym ruchem naprawiło zarówno save/load, jak i osobno znaleziony brak carry przez in-session `rebuildWorldBundle`. Nowe `SaveData.terrainModifications` persystuje tylko wpisy `source: 'player'` — `'system'`-owe (rzeźbienie jaskiń `createLargeCaves.ts`, replay wypalenia spawn pointu w `createFauna.ts`) są deterministyczne i odtwarzane od zera przy każdym `buildWorldSystems()`, więc nigdy nie są zapisywane (inaczej ich kumulatywna głębokość podwoiłaby się przy każdym reloadzie). Przy okazji: `Wyrównaj` dostało stabilne, zależne od lokalizacji id (zamiast `Date.now()`/`Math.random()` za każdym razem), żeby powtarzane wyrównywanie tego samego miejsca podmieniało swój wpis zamiast rosnąć bez końca w trwałym teraz zapisie; martwy tryb `'level'` (`applyModificationToTile`) usunięty przy okazji dotykania tego typu.
- [ ] 2026-08-24 — plan 178 (Hunter): gospodarstwo Huntera zbiera nadwyżkę (mięso/skóra/strzały) w nowym `Household.items` (generyczny `Inventory`, nie `EconomicStock`), ale nic jej stamtąd nie wyprowadza do handlu. `SettlementEconomy`'s `wood`/`iron`/`coal`/`gold` mają ten sam problem od dawna — merchant/`tradeCatalog.ts`/`trade.ts` to nieskończona abstrakcyjna lista cen, w ogóle niepodłączona do żadnego stocku osady/gospodarstwa. Brakuje generycznego mostu "household/settlement item surplus → sprzedawalny stock kupca"; hide/bow/arrow/dried_meat są już sprzedawalne graczowi wprost (plan 162, bez zmian), ale to nie to samo co NPC-owy nadmiar faktycznie zasilający ekonomię. Osobny, przekrojowy plan ekonomii, nie doklejka do 178 (`2026-08-20--178--hunter-profession-and-household.md`, implementation notes §18).
- [ ] 2026-08-24 — plan 178 (Hunter): gotowanie (`items/campfireCooking.ts`) i suszenie (`world/dryingRacks.ts`) są, przed i po tym planie, w 100% wyzwalane wyłącznie przez gracza — żadna rola NPC (w tym żona Huntera) nie ma ścieżki, która by z nich korzystała. Upolowane mięso/skóra trafiają do `household.items` i tam zostają (dostępne pod przyszły plan). Zbudowanie generycznej zdolności "NPC wykonuje timed action przy obiekcie świata (ognisko z rusztem / suszarnia) na stocku gospodarstwa" to nowy, przekrojowy system NPC-daily-life, nie mały dodatek do 178 (`2026-08-20--178--hunter-profession-and-household.md`, implementation notes §18).
- [ ] 2026-08-27 — `items/primaryWeapons.ts` (plan `ui-input-002` §6, primary melee/ranged HUD shortcuts) jest świadomie session-local, nie w `SaveData` — po reloadzie gry pamięć "ostatnio założona broń danego typu" resetuje się do pustej i odbudowuje się od nowa przy pierwszym `equip()`. Analogiczne do `heldTool`, które JEST persystowane (`SaveData.heldTool`); jeśli graczom przeszkadza reset po Continue, dodać `primaryMeleeKind`/`primaryMeleeInstanceId`/`primaryRangedKind`/`primaryRangedInstanceId` do `SaveData` tym samym wzorcem.
- [ ] 2026-08-27 — plan settlements-npcs-002 (NPC professions): Farmer's plant branch (`NpcAgent.beginFarmWork`) and Blacksmith's sharpen branch (`beginBlacksmithWork`) are both fully wired but effectively dormant today — no gameplay path deposits `seed_carrot`/`seed_potato`/`seed_cabbage` or `whetstone` into a `Household`'s `items` (both are player-tradeable-only items). In practice Farmer only ever harvests, and Blacksmith only ever falls back to the idle work stand. Needs a generic "household receives an item" path (gift/delivery/production) before either branch does anything in a normal playthrough (`settlements-npcs-002-npc-professions-complete-profession-work-integration.md`).
- [ ] 2026-08-27 — plan settlements-npcs-002 (Trader): `beginTraderWork` only ever moves the trader's own household's `food`/`wood` surplus into `SettlementEconomy` when the settlement has a matching shortage — deliberately bounded (plan §7 "nie tworzyć jeszcze pełnego globalnego market simulation"), not cross-household redistribution or real barter. A future economy plan could extend this to read other households' surplus (would need a `HouseholdRegistry`-shaped hook injected into `NpcAgent`, which doesn't exist today) or wire `items/trade.ts`'s barter primitives into a two-sided NPC transaction.
- [ ] 2026-08-27 — plan settlements-npcs-002 (Farmer): the plan text's "dry crop → water" branch has no code counterpart — `world/cropLifecycle.ts` (plan 172) is a pure `(seed, worldDays)` function with no hydration state at all; watering/hydration is a separate, still-`planned` plan (`settlements-npcs-001-cultivation-hydration-and-watering.md`). Farmer v1 only harvests/plants; revisit once/if 001 lands.
- [ ] 2026-08-27 — plan `ui-input-002` §2's shared interaction-panel `actions` (nowy `FlavorDialogState.actions`, `InteractionPanelAction`) jest dziś podłączony tylko do `playerWell` (`[R]` — wymagania budowy) i biernie dostępny dla istniejącej generycznej `resolveInteraction()`+`npcDialog.open()` ścieżki (house/householdStorage/landmark/settlementStorage/well/tree/animal/spawner flavor branches — dziś zawsze z pustą `actions: []`, więc wyglądają dokładnie jak przed zmianą). Pozostałe `Interactable.kind`, które dziś odpalają akcję natychmiast na `[E]`+toast (dig/trap/campfire/dryingRack/hive/crop/gardenPlot/container/deposit/corpse/landPlot/waterEdge/item/terrainPreparation), świadomie nie zostały przepięte — każdy z nich to pojedyncza akcja bez realnej korzyści z listy przycisków, a przepinanie ich zmieniłoby responsywność jednoklawiszowej pętli bez możliwości przetestowania w przeglądarce w tej sesji. Jeśli audyt UX pokaże realną potrzebę (np. `corpse`, gdy `target.action` kiedyś przestanie być jednoznaczne), podłączyć przez ten sam kontrakt zamiast nowego.
- [ ] 2026-08-28 — plan fauna-002 (livestock food production) §12 chce persystencji stanu produkcji (jajko/mleko cooldown) w `SaveData`. Świadomie pominięte: `ARCHITECTURE.md`'s Save schema sekcja jest wprost — "fauna/livestock HP/death/corpse state is not persisted at all (killed animals resurrect on reload)" — żaden `AnimalAgent` (dziki ani gospodarski) nie ma dziś żadnego per-instance runtime state w `SaveData`, ani nawet w in-session `WorldBundle`-rebuild registry (w przeciwieństwie do `Household`/NPC state, które MAJĄ taki registry, ale i tak nie są częścią `SaveData`). Dodanie persystencji tylko dla produkcji, przy braku persystencji śmierci/HP tego samego `AnimalAgent`, byłoby niespójne (kura mogłaby "zmartwychwstać" po reloadzie, ale jej cooldown jajka miałby przetrwać) i realną nową zdolnością architektoniczną, nie małym rozszerzeniem M-sized planu. Timer produkcji używa za to absolutnych kotwic `elapsedDays` (`fauna/livestockProduction.ts`), więc poprawnie liczy się po dowolnie długim czasie offline w tej samej sesji — resetuje się tylko po pełnym reloadzie gry, tak jak `life.hunger`/`life.thirst` już dziś. Jeśli przyszła praca chce prawdziwej persystencji fauny/livestock (śmierć+HP+potrzeby+produkcja razem), to osobny, świadomy plan architektoniczny, nie rozszerzenie tego (`fauna-002-livestock-food-production-implementation-notes.md`).
- [ ] 2026-09-03 — po naprawie offsetu wejścia jaskini (`caveGenerator.ts`, `CAVE_MOUTH_DEPTH`) zostają dwie rzeczy z planu `world-terrain-007` do kalibracji/dokończenia: (1) **gęstość** — `pickLargeCaveSites` proponuje ~10 miejsc na świat, ale filtr overburden przepuszcza średnio ~2 (pomiar na 40 seedach analitycznym `sampleHeightAt`; przed naprawą było ~0.1, czyli praktycznie brak jaskiń); plan §5 przewidywał spike kalibracyjny, który nigdy nie powstał. (2) **ergonomia wejścia** — „otwór” to nadal tylko dwa `modifyTerrain` (dołek r=1.65/2.4 m + podejście r=3.2/1.35 m) przy rozdzielczości siatki 1 m, więc wejście do jaskini to faktycznie wpadnięcie do zagłębienia, a nie wejście w zbocze; plan §7 („wejście jest jedynym normalnym połączeniem surface ↔ cave”) zasługuje na prawdziwy, wyrzeźbiony wjazd/rampę zamiast dziedziczonego po `createLargeCaves.ts` dołka.
