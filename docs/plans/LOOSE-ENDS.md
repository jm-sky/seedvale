# Seedvale — Loose Ends

Krótkie, jednowierszowe notatki o pobocznych blokadach, pomysłach i wątkach
do dokończenia, które pojawiają się podczas implementacji planu, a nie
mieszczą się w jego zakresie. To nie jest issue tracker — gdy wpis dojrzeje
do realnej pracy, przenieś go do `docs/issues/` (nowy plik + wiersz w
`docs/issues/README.md`) i usuń stąd. Wpisy nieaktualne / już nieistotne też
usuwaj — lista ma zostać krótka.

Format: `- [ ] YYYY-MM-DD — opis (plan/plik, jeśli istotne)`

## Wpisy

- [ ] 2026-08-21 — `Settlement` nie eksponuje `GardenScale` per `landmarks.gardens[i]` (tylko pozycje), więc walidacja sadzenia crop-a (`world/plantedCrops.ts`'s `isNearAnyGarden`) używa jednego konserwatywnego promienia (`gardenClearingRadius('L')`) zamiast dokładnego footprintu S/M/L danego ogródka — S/M ogródek dopuszcza sadzenie nieco dalej niż jego realne grządki. Wymagałoby przeniesienia `gardenScale`/`plan.landmarks` na `Settlement`/`SettlementLandmarks` (plan `2026-08-16--126--seed-planting.md`)

- [x] 2026-08-16 — `NpcAgent.getCurrentActivity()` pokazuje ore-deliver leg (`deposit` po `mine`) jako `need: idle` zamiast `work`, bo chain traci informację o rodzicu po promocji `next` → `pendingAction`; kosmetyczny błąd etykiety dialogowej, bez wpływu na gathering/ekonomię (plan `2026-08-16--131--natural-resource-gathering.md`) — naprawione 2026-08-18: `NpcPlannedAction.chainKind` przenosi klasyfikację rodzica przez promocję `next` (`NpcAgent.ts`'s `promoteChainKind`/`classifyPendingActivity`, testy w `src/ai/npcCurrentActivity.test.ts`); wood chop→deposit (need-driven) bez zmiany zachowania
- [x] 2026-08-16 — spawn-point lifecycle (`PreySpawner.state`/`deathsThisCycle`/`disabledAtDay`) nie jest persystowany; reload po `[E] Zniszcz` odradza wypaloną spawn point z powrotem jako `active` (dead reckoning z seeda, tak jak przed planem 125). Zamierzone ograniczenie zakresu L-effortu (plan nie testuje tego w kryteriach akceptacji) — jeśli konsekwencja ma przetrwać reload, potrzebna jest zwarta kolekcja w `SaveData` keyed by `PreySpawner.id` (plan `2026-08-16--125--fauna-spawn-point-population-limits.md`, patrz implementation notes §Persistence) — naprawione 2026-08-18: `SaveData` v17 `spawnPoints` (`persistence/saveData.ts`), `AnimalSpawner.ts`'s `snapshotSpawnPointState`/`restoreSpawnPointState`, wired przez `createFauna`/`worldBundle.ts` (save/load **i** in-session `rebuildWorldBundle`); `disabled`/`recovering` restore nie odtwarza zwierząt i ponownie przypala prop/teren; brak testu w przeglądarce
- [ ] 2026-08-19 — jaskinia jest siedliskiem drapieżników (`cave` → `wolf`); niedźwiedź ma tam trafić, gdy gatunek powstanie — nie dodawać osobnego spawn pointu, tylko `SPAWNER_SPECS` / `kind` (plan 125 follow-up)
- [ ] 2026-08-17 — vegetation to **311 `InstancedMesh`ów na 709 instancji** (2,3 instancji na draw call): `buildInstancedProps` batchuje per `(chunk, species, primitive)`, więc instancing prawie nic nie daje. Merge cross-chunk to największa dostępna redukcja draw calls i dotyczy każdego passa — ale to realny refactor `render/instancedProps.ts` + `terrain/chunkManager.ts` (research `2026-08-17--019--rendering-optimizations.md` §4.3)
- [ ] 2026-08-17 — `N8AOPass` renderuje scenę **drugi raz** (własny depth/normal override) wewnątrz `RENDER`. `N8AOPostPass` (reuse depth texture) to alternatywa, ale zmienia architekturę renderu i dotyka podejrzanego o `GL_INVALID_OPERATION` MRT handlingu n8ao vs three@0.185 (research `2026-08-17--019--rendering-optimizations.md` §4.2, `2026-08-17--015--streaming-hitch-gl-errors-handoff.md`)
- [x] 2026-08-17 — repo ma dwa lockfile'e: `packageManager: pnpm@11.20.0` + `pnpm-lock.yaml`, ale CI robi `npm ci` na `package-lock.json`. Rozjazd już raz wywalił CI na `main` (n8ao 2.0.0 vs 2.0.1); zresynchronizowane, ale rozjedzie się znowu — wybrać jeden menedżer (research `2026-08-17--017--threejs-rendering-audit.md` §4 F8)
- [ ] 2026-08-17 — melee ma dwa niezależne pojęcia „kierunku ataku”: `player.faceToward()` obraca mesh (i robi to jeszcze **przed** `gapClose()`, więc po kolizyjnym zsunięciu mesh patrzy lekko obok), a hit test (`resolveMeleeHits`) liczy się z yaw kamery. Plan 142 pogodził je tylko dla touch (`attackYaw`); desktop nadal może wizualnie patrzeć w cel i chybić, jeśli kamera odjedzie w trakcie wind-upu (plan `2026-08-17--142--mobile-combat-target-acquisition.md`)
- [ ] 2026-08-17 — pułapka podniesiona (`[R] Zabierz`) wraca do ekwipunku z pełną trwałością: `Inventory` jest count-based, więc nie ma gdzie zapisać `remainingDurability` przedmiotu. Drobny exploit (zdjąć na 0.1 trwałości → postawić jak nową); wymagałby per-instance item state, co jest poza zakresem planu 141 (plan `2026-08-17--141--animal-traps.md`)
- [ ] 2026-08-17 — pułapki łapią tylko w zasięgu aktywnej symulacji fauny (`Fauna.getAgents()`); zwierzęta nie są symulowane poza załadowanym obszarem, więc pułapka zostawiona daleko od gracza nie „doliczy” złapań wstecz. Zamierzone ograniczenie zakresu planu 141 §19 — pełny off-screen trapping wymaga symulacji fauny dla nieaktywnych chunków (plan `2026-08-17--141--animal-traps.md`)
- [ ] 2026-08-19 — plan 149 Phase 1 A nie prewarmuje MeshDepthMaterial / EffectComposer (~26–27 programów na frame 0). Pierwszy draw już zlinkowanych programów sceny nadal kosztuje ~180–420 ms (`onFirstUse`) za overlayem loadera. Nie dokładać drugiej warstwy `compileAsync` bez osobnego małego planu (`2026-08-17--149--shader-program-first-use-hitch.md` §19)
- [ ] 2026-08-21 — Etap 2 kandydaci po podziale `createApp.ts` (Etap 1): `src/app/gameLoop.ts` (~1640 linii — tick simulation + interakcje `[E]`/`[R]` + melee/ranged rozstrzyganie + day/night/weather apply + render/minimapa w jednym pliku) i `src/app/interactables.ts` (~720 linii — jeden `buildInteractables` obsługujący ~20 rodzajów kandydatów). Oba to osobna, świadoma robota, nie doklejka do Etapu 1
- [ ] 2026-08-21 — "Wyrzuć" dla instance-backed kinds (siekiera/nóż/sierp/pułapki — `isInstanceBackedKind`) teraz działa (`inventoryWiring.ts`'s `dropItemStack`, `InventoryScreenItemList.vue`/`InventoryScreenItemDetails.vue`), ale świadomie tylko jako zwykły item: `bundle.droppedItems.drop(kind, x, z)` nie niesie `durability`/`sharpness` instancji, więc wyrzucony i ponownie podniesiony nóż wraca ze świeżą, domyślną kondycją zamiast tej, jaką faktycznie miał. Wymaga instance-aware world-drop (props na dropniętym itemie + ich odczyt przy podniesieniu), zanim to przestanie być stratą informacji
- [ ] 2026-08-21 — plan 181 Etap 4–6 (sieć rzeczna jako tile'e 256m + halo, chunk-continuous ribbon geometry, lekki water material) zaimplementowane; Etap 7 (deterministyczne world-space meandrowanie z taperem do 0 na krawędzi tile'a, drugi pass Chaikin, krzywa szerokości/`flowFactor` z mocniejszym ease-in dla małych strumieni, per-vertex `aFlow` sterujący alpha/foam/bank-fade w shaderze) zaimplementowane 2026-08-21. Nadal otwarte: wodospady, pełna parytetowość shadera z jeziorem/oceanem (reflection binding, depth-based foam), worker offload dla obliczeń tile'a hydrologii (obecnie main-thread, ~18ms/tile zmierzone syntetycznie), oraz interaktywny debug-overlay do wizualnej inspekcji hydrologii w przeglądarce (plan sugerował to jako opcję, wciąż niezrobione) (`2026-08-21--181--natural-mountains-and-rivers.md`)
- [ ] 2026-08-21 — `src/items/trade.ts`'s `wouldFitAfter()`/`wouldFitAfterInstance()` (używane przez `buyWithCoins`/`buyWithBarter`) sprawdzają tylko wagę, nie gabarite (`maxSize`, plan 164). Dla stackable kind (nieinstancyjny branch `buyWithBarter`/`buyWithCoins`) wynik `inventory.add(kind, 1)` jest też ignorowany — jeśli tylko limit gabarite (nie waga) zostałby przekroczony, transakcja i tak zwróci `'ok'` (monety/oferta znikają, przedmiot faktycznie nie trafia do ekwipunku, bo `Inventory.add()` wewnętrznie i tak odrzuca przez `canAdd()`). Nieodtworzone w praktyce, znalezione przy okazji naprawy `isValidOffer` dla instance-backed barter offers (patrz `git log` `src/items/trade.ts`, 2026-08-21) — wymaga sprawdzenia `canAdd`/dodania gabarite do `wouldFitAfter*` + użycia zwracanej wartości `add()`/`addInstance()` w stackable branchu
