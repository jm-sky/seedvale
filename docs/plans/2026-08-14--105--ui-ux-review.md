# Plan 105: audyt UI/UX

**Status:** `in progress` 🟡 — **audyt `done`** (review [007](../reviews/2026-08-14--007--ui-ux.md)); H1 (5/5) + H2 (2/3) + nowy ekran Character zaimplementowane technicznie, **bez weryfikacji w przeglądarce** (§11); H2.1, H3, H4 nadal otwarte.
**Created:** 2026-08-14 · **Audyt:** 2026-08-14
**Priority:** 🟡 medium · **Effort:** L (audyt ✅) + implementacja wg faz H
**Depends on:** ~~046~~ ~~005~~ ~~023~~
**Źródła (wiążące):**
- [prompt 003 — UI review](../prompts/2026-08-14--003--ui-review.md)
- [review 007 — UI/UX](../reviews/2026-08-14--007--ui-ux.md) (A–H; numer 006 zajęte przez architecture-alignment)
- Plan 046 (Vue + Tailwind, Fazy 0–4 zaimplementowane, `verification needed`)
- Plan 005 (ekrany), plan 023 (touch)

> **Charakter:** review warstwy interfejsu (UX, visual, responsive, Vue/CSS, Tailwind/shadcn). Audyt zamknięty w review 007. **Implementacja = sekcja 8 (H)** — bez równoległego systemu UI.

---

## 1. Cel

Kompleksowy audyt UI/UX Seedvale na desktopie i mobile. Nie tylko CSS: ekrany, HUD, kontrolki, inventory, panele, stany (hover / disabled / empty / error).

Priorytet: **UX**. Immersja / „mniej webowo” jest ważne, ale drugorzędne.

Kluczowe są ekrany i kontrolki. Dialogi i interakcja ze światem mogą iść drugą fazą. Night UI — później.

## 2. Deliverable

| Plik | Rola |
|------|------|
| ten dokument | kanoniczny plan; fazy H poniżej |
| [review 007](../reviews/2026-08-14--007--ui-ux.md) | raport A–H (audyt 2026-08-14) |
| `_temp/ui-review/` | zrzuty (startowe + sesja browser) |

Język: polski. Severity: Critical / High / Medium / Low. Każdy problem: obszar, dlaczego, rekomendacja, plik, screenshot jeśli jest.

Rozszerzać istniejący store + ekrany Vue. Nie tworzyć równoległego systemu UI.

## 3. Hipotezy (do weryfikacji, nie findings)

Z kodu, startowych zrzutów i sugestii użytkownika:

- HUD na landscape nadal pokazuje `Joystick = ruch` — hint chowa się tylko przy `max-[700px]` (`HudScreen.vue`), A55 w poziomie jest szerszy. Źródło: `HUD_HINT_TOUCH` w `src/ui-vue/store.ts`. Duplikat w pauzie i Notatkach.
- Quick Actions na mobile nachodzi na pauzę, `+` minimapy i paski HP — Pause / Action cluster / Minimap to trzy niezależne `fixed` kontenery (`TouchChrome.vue`, `MinimapScreen.vue` `top: max(68px, …)`).
- Status `Brak surowców` siedzi w guziku zamiast toastu (`QuickActionsScreen.vue`); toast już istnieje (`ToastStack.vue`, issue [012](../issues/2026-08-10--012--toast-notifications-for-quick-feedback.md)).
- Quick Actions i Pauza → Akcje pokazują pełną listę, nie tylko dostępne akcje; duplikacja logiki ognia.
- Mix PL/EN: `Resume` / `Save` / `New Game` / `Character` vs `Zadania` / `Ekwipunek`.
- FPS i waga w HUD wyglądają jak debug chrome.
- shadcn-vue jest **zainicjowany, nie użyty**: `components.json` + `cn()` + tokeny, ale brak `src/ui-vue/components/ui/*`. `@theme` ma ciemne `--color-panel` / `--color-ink`, a shadcn `:root` to jasny `--background: oklch(1 0 0)` na `body`.
- Martwy CSS `.seedvale-pause*` itd. w `index.html` po migracji Vue.
- Joystick zostaje vanilla DOM (`createTouchControls.ts`) — zamierzone.

Powiązane issue’e — sprawdzić, nie duplikować: 005 (ikony), 006 (lista mieszkańców), 007 (etykiety NPC nad modalami), 012 (toasty).

Sugestie z promptu (weryfikować w przeglądarce):

- toast zamiast statusu w guziku Quick Actions
- ikony tam, gdzie pomagają
- usunąć `Joystick = ruch` z HUD
- wspólny flex container: Pause + Action cluster + Minimap
- Quick Actions / Akcje: tylko aktualnie dostępne akcje

## 4. Metoda

1. Inwentarz kodu — ekrany z `src/ui-vue/App.vue`, facady `src/ui/create*.ts`, leftover CSS, CSS2D labele, lil-gui. Nie oceniać wyłącznie CSS.
2. Cursor Browser na `localhost:5577` (nie headless Chrome/Playwright). Viewports:
   - desktop ~1920×1080
   - Galaxy A55 **poziomo** (1080×2340 → landscape; CSS ok. 832×384, nie 2340 CSS px)
3. Porównanie implementacji z realnym wyglądem.
4. Raport 006 z evidence. Hipotezy i sugestie użytkownika — findings albo odrzucenie z powodem.
5. Sekcję H wciągnąć z powrotem do tego planu (poniżej), bez osobnego numeru.

## 5. Walkthrough

### Faza 1 — ekrany i kontrolki (główny tor)

Zawsze widoczne: HUD, minimapa, toast, touch chrome + joystick, prompt `[E]` (`FlavorDialog.vue`).

Modale / panele (otwarty, empty jeśli istnieje, close/Esc, mobile vs desktop):

- Pauza: main / Akcje / Ustawienia
- Ekwipunek: lista + details
- Quick Actions (desktop FAB + mobile ⚡)
- Zadania, Mieszkańcy, Mapa świata, Notatki, Świat (world config)
- Busy overlay, time-skip (jeśli da się wywołać bez długiej rozgrywki)

Stany: hover/active (desktop), disabled/unavailable, selected (held tool), loading (busy), empty (inventory/quests), error (toast / brak surowców).

### Faza 2 — dialogi / świat (lżejsza; inaczej follow-up)

- dialog NPC, handel, flavor dialog, gaze prompt przy obiekcie
- lil-gui tylko jeśli widać bez `?debug=1` / Toggle debug panel

## 6. Kryteria oceny

- **UX:** hierarchia, czytelność, discoverability, feedback, modalność, ile UI zasłania świat 3D, różnica mobile vs desktop.
- **Visual:** typografia, spacing, radius, kolory, kontrast, ikony, panele, z-index. Szukać bliźniaczych elementów o różnych stylach.
- **Mobile:** touch targets ≥44px, joystick vs E vs pauza vs minimapa, safe area, landscape A55, czy modale są używalne.
- **A11y (rozsądny zakres):** kontrast, focus, keyboard, informacja nie tylko kolorem. Semantyka HTML / aria — poza zakresem (skreślone w prompcie).
- **Vue/CSS:** duplikacja, hardcoded values, brakujące wspólne komponenty; co CSS (joystick, canvas minimapy, CSS2D), co Tailwind, czy cienka warstwa tokenów wystarczy.
- **shadcn-vue:** nie zakładać migracji wszystkiego. HUD/game chrome zostaje custom; panele/formularze mogą użyć primitive (Button, Dialog, Input). Nie drugi system obok store + facades.

## 7. Struktura raportu (review 006)

- **A.** Executive summary
- **B.** UI inventory
- **C.** Problems
- **D.** Design system — bazować na `--color-panel` / `--color-ink` / `--radius`
- **E.** Responsive strategy
- **F.** Vue/CSS architecture
- **G.** Tailwind: migrate / keep as CSS / refactor first / do not touch
- **H.** Implementation plan — tu i w tym pliku (sekcja 8)

## 8. Fazy implementacji (z review 007)

Findings: C1–C12 w [review 007](../reviews/2026-08-14--007--ui-ux.md). Kolejność = UX first.

### H1. Quick wins (S–M)

| # | Zmiana | Finding | Pliki (orientacyjnie) |
|---|--------|---------|------------------------|
| 1 | Ukryć touch HUD hint (`isTouchDevice` / `(pointer: coarse)`), nie tylko `max-[700px]` | C1 | `HudScreen.vue`, `store.ts` |
| 2 | PL copy: start + pauza + settings (`Continue`→Kontynuuj, `Resume`→Wznów, `Save`→Zapisz, `New Game`→Nowa gra, `Saved`→Zapisano, Character/Name) | C5 | `createStartScreen.ts`, `PauseMenuEntriesMain.vue`, `PauseMenuEntriesSettings.vue` |
| 3 | Status akcji → `showToast` (error/info); usuń `:status` z guzików QA / Pause Akcje | C3 | `QuickActionsScreen.vue`, `PauseMenuEntriesActions.vue` |
| 4 | Filtruj akcje ognia po dostępności (jak shovel/tent/nearTown) | C4 | te same + `userActions` |
| 5 | FPS poza domyślnym HUD (debug / ustawienie) | C6 | `HudScreen.vue`, `setHudFps` |

### H2. High impact (M–L)

| # | Zmiana | Finding |
|---|--------|---------|
| 1 | Wspólny flex right-column: Pause + Minimap + Action cluster; QA jako bottom sheet na niskim `dvh` | C2 |
| 2 | Inventory: touch targets ≥44px; mniejsza gęstość listy | C7 |
| 3 | Skrócić / schować długi desktop hint (Notatki = kanon) | C6 |

### H3. Structural (L)

| # | Zmiana | Finding |
|---|--------|---------|
| 1 | Shared `playerQuickActions` + cienkie `UiButton` / `UiPanel` | C8 / F |
| 2 | Dark tokens jako prawda; light shadcn `:root` nadpisać lub usunąć; opcjonalnie shadcn Button/Input tylko dla formularzy | C9 |
| 3 | Cleanup martwego CSS Vue-migracji w `index.html` (zostaw touch/loading/labels/start) | C10 |
| 4 | lil-gui domyślnie ukryty; odsłanianie z Ustawień / `?debug=1` | C11 |

### H4. Later polish

- Ikony itemów / chrome (issue 005)
- Night contrast HUD (C12)
- Faza 2 review: dialogi NPC, handel, flavor, gaze
- Immersja („mniej webowo”) po H1–H3

**Reguła:** rozszerzać store + ekrany Vue; nie tworzyć równoległego UI.

## 9. Poza zakresem sesji review

- Zmiany w `src/` / CSS
- Nocne warunki
- Pełna interakcja ze światem jako główny tor
- Nowe modele / dźwięki
- Commit

## 10. Weryfikacja review

Raport kompletny, gdy:

- inwentarz pokrywa `App.vue`
- desktop + A55 landscape mają zrzuty kluczowych ekranów
- sugestie użytkownika są findings albo odrzucone z powodem
- sekcja H tego planu da się wykonać bez zgadywania architektury

---

## Uwagi 

- Rozważyć i18n dla PL i EN

---

Zrobić przy okazji:

## Character Screen — UI Refactor Plan

### Cel

Dodać ekran **Character** opisujący bohatera/gracza oraz uporządkować prezentację jego aktualnego stanu w ramach refaktoru UI.

Pierwsza wersja pokazuje:

- HP
- Hunger
- Thirst
- Vigor
- Dirtiness

### Zakres

#### 1. Character screen

Dodać osobny ekran/panel "Character" zgodny z istniejącą architekturą UI.

Dla każdej wartości pokazać:

- nazwę
- aktualną wartość
- maksymalną wartość, jeśli dotyczy
- wizualny stan krytyczny/niski

#### 2. Player state

Character UI ma korzystać z istniejącego stanu gracza.

Nie tworzyć osobnych wartości potrzeb przechowywanych w UI.

Preferowany przepływ:

"Player state → Needs/Health → Character UI"

Jeżeli istniejące systemy NPC mogą zostać bezpiecznie wykorzystane również przez Playera, należy je współdzielić zamiast tworzyć równoległą implementację.

#### 3. Needs

Pierwszy zestaw potrzeb:

- "Hunger"
- "Thirst"
- "Vigor"
- "Stamina" (?) 
- "Dirtiness" (nowe, dodać w osobnym planie) 

"HP" pozostaje częścią systemu zdrowia, a nie potrzeb.

"Dirtiness" traktować jako stan/need, który w przyszłości może wpływać na zachowanie, higienę, komfort i relacje z NPC. Nie dodawać w tej sesji. 

#### 4. UI architecture

Character powinien być przygotowany na późniejsze rozszerzenia:

- Traits
- Skills
- Equipment
- Relationships
- Injuries
- Fatigue
- inne informacje o bohaterze

UI ma być warstwą prezentacji, a nie źródłem prawdy dla stanu gracza.

#### 5. Multiplayer readiness

Architektura nie powinna zakładać, że UI może bezpośrednio modyfikować statystyki.

Stan Character powinien być możliwy do późniejszego przeniesienia do modelu server-authoritative:

"Server/Simulation → Player State → Character UI"

Nie implementować jeszcze multiplayera ani server-side simulation.

### Poza zakresem

- pełny system Player AI
- nowe zachowania gracza
- multiplayer
- server migration
- rozbudowany system equipment
- nowe questy
- nowe mechaniki związane z higieną

### Kryteria ukończenia

- [x] Istnieje ekran "Character" (`CharacterScreen.vue`, "Postać" w menu pauzy).
- [x] Ekran pokazuje HP, Hunger, Thirst, Vigor. **Dirtiness pominięte** — §3 tego dokumentu mówi wprost "nie dodawać w tej sesji" (osobny plan), mimo że ta lista w §Cel go wymienia; kod (`DirtinessState`) nie istnieje.
- [x] Dane pochodzą bezpośrednio z authoritative Player state (`player.health` / `player.needs`, plan 106), pchane raz/klatkę przez `Hud.setCharacterStats` — ten sam wzorzec co istniejące `setPlayerNeeds`.
- [x] UI nie przechowuje własnego stanu potrzeb — `ui.characterScreen` to czysty odbiornik, ekran niczego nie zapisuje z powrotem.
- [x] Wykorzystano istniejące systemy (`HealthState`, `PlayerNeeds`) zamiast równoległych.
- [x] Architektura gotowa na rozszerzenie — wiersze statystyk generowane z listy (`rows` computed), nie zakodowane na sztywno w template; dodanie Traits/Skills/itd. to nowe wpisy/sekcje, nie przebudowa.
- [x] Rozwiązanie nie utrudnia migracji server-authoritative — ekran tylko czyta, nic nie mutuje.

**Nie zweryfikowano w przeglądarce** — patrz §11 poniżej.

---

## 11. Implementation notes (2026-08-14, sesja druga)

Zakres tej sesji: wybrane pozycje H1–H2 (nie wszystkie) + nowy ekran Character. H3/H4 poza zakresem — nie ruszane.

### Zaimplementowane

**H1 (wszystkie 5)**
1. C1 — `HudScreen.vue`: hint dotykowy chowany przez `v-if="!isTouchDevice()"`, nie tylko `max-[700px]`. Rozwiązuje przypadek A55 landscape (~832px).
2. C5 — PL copy: `createStartScreen.ts` (Kontynuuj/Nowa gra), `PauseMenuEntriesMain.vue` (Wznów/Zapisz/Zapisano/Nowa gra), `PauseMenuEntriesSettings.vue` (Postać/Imię/Panel debug), `createApp.ts`'s `window.confirm(...)`.
3. C3 — status akcji ognia (Quick Actions + Pauza→Akcje) → `showToast(...)`; usunięto `:status` z `QuickActionsButton`/przycisków pauzy. Rozszerzone też na status odpoczynku (obóz/miasto) w Quick Actions, zgodnie z H1.3's ogólnym sformułowaniem ("usuń `:status` z guzików QA / Pause Akcje").
4. C4 — 4 akcje ognia (gałąź/pochodnia/palenisko/ognisko) filtrowane po dostępności. Nowe predykaty `canBuildSimpleFire`/`canBuildFirePit`/`canLightBranch`/`canLightWoodenTorch` w `userActions.ts`; pchane do store przez `syncQuickActionAvailability` (przemianowane z `syncShovelQuickActions` — teraz to jeden hak wywoływany po każdej mutacji ekwipunku, tak jak wcześniej dla shovel/tent). Quick Actions i Pauza→Akcje czytają to samo `ui.quickActions.fireAvailability` (uniknięto duplikowania stanu, choć logika przycisków w obu plikach zostaje osobna — pełne zjednoczenie to H3/C8, poza zakresem).
5. C6 — FPS domyślnie ukryty (`ui.hud.showFps`, toggle "FPS w HUD" w Ustawieniach); długi hint desktopowy skrócony do wskazania Notatek jako kanonu.

**H2 (2 z 3 — wybrane)**
- C7 — `ItemsScreenItemButton.vue`: `min-h-11` (44px) na przyciskach Weź/Odłóż/Wyrzuć/Zjedz-Wypij (lista + szczegóły ekwipunku).
- C6 — skrócenie hintu HUD (patrz H1.5 wyżej, ta sama zmiana pokrywa H2.3).
- **Pominięte: H2.1** (wspólny flex right-column: Pause + Minimap + Action cluster, C2) — najbardziej ryzykowna, strukturalna zmiana dotykająca trzech niezależnych `fixed` kontenerów (`TouchChrome.vue`, `MinimapScreen.vue`, `QuickActionsScreen.vue`); wymaga weryfikacji na realnym landscape mobile, której nie da się wykonać z tej sesji (patrz CLAUDE.md — nie uruchamiam headless Chrome/Playwright do weryfikacji wizualnej). Zostawione na osobną sesję z weryfikacją w przeglądarce przez użytkownika.

**Nowy ekran Character**
- `src/ui-vue/screens/CharacterScreen.vue` (nowy) — modal w stylu `NotesScreen.vue`; pokazuje HP + Kondycja/Wigor/Głód/Pragnienie (nazwa, aktualna/maks. wartość, pasek, stan krytyczny <20%).
- `ui.characterScreen` w `store.ts` (`openCharacterScreen`/`closeCharacterScreen`/`isCharacterScreenOpen`/`setCharacterStats`), forwardowane przez `mount.ts` tylko tam gdzie wołane spoza Vue (`setCharacterStats`).
- `Hud.setCharacterStats` (`createHud.ts`) + wywołanie w `gameLoop.ts` obok istniejącego `hud.setPlayerNeeds(...)` — czyta `player.health`/`player.needs` (plan 106), nic nowego nie liczy.
- Wpis "Postać" w głównym menu pauzy (`PauseMenuEntriesMain.vue`), między Ekwipunkiem a Mapą. Bez własnego skrótu klawiszowego — spójne z Villagers/Notes/WorldConfig (też tylko przyciskiem).
- Dirtiness świadomie pominięte (patrz kryteria ukończenia wyżej).

### Poza zakresem tej sesji (świadomie)

- H3 (structural): shared `playerQuickActions`/`UiButton`/`UiPanel` (C8), dark-token vs shadcn `:root` (C9), cleanup martwego CSS (C10), lil-gui domyślnie ukryty (C11).
- H4 (later polish): ikony itemów, night contrast HUD, Faza 2 (dialogi NPC/handel/flavor/gaze).
- H2.1 (layout collision fix, C2) — patrz wyżej.

### Weryfikacja

**Techniczna (zrobione, wszystko przechodzi):**

    npx tsc --noEmit   → OK
    npm run lint       → OK (jedyne błędy to preexisting _temp/asset-audit/inspect.mjs, niezwiązane z tą sesją)
    npm run build      → OK
    npm run test       → 567/567 passed

**Browser/manual — NIE wykonano w tej sesji.** Do sprawdzenia przez użytkownika na działającym `npm run dev`:

1. Landscape telefon (lub DevTools device toolbar ~832×384 z touch emulation) — hint `Joystick = ruch` już się nie pokazuje.
2. Zbuduj/zapal coś bez wystarczających zasobów z dala od Quick Actions/Pauzy → Akcje — przycisk nie powinien być widoczny (a nie: widoczny + błąd w guziku); usuń jedyny potrzebny surowiec i sprawdź, że przycisk znika z panelu otwartego wcześniej po następnej mutacji ekwipunku.
3. Zbuduj ognisko/zapal gałąź skutecznie — toast z potwierdzeniem, nie tekst w guziku.
4. Otwórz Pauza → Postać — HP/Kondycja/Wigor/Głód/Pragnienie pokazują sensowne wartości i aktualizują się na żywo (zgłodnij/napij się/odpocznij i sprawdź zmianę).
5. Zejdź z dowolną wartością poniżej 20% (np. zaczekaj z głodem) — wiersz podświetla się na czerwono z etykietą "krytyczne".
6. Ustawienia → "FPS w HUD" — domyślnie wyłączone; po włączeniu FPS pojawia się w HUD.
7. Start/pauza/ustawienia — cały tekst na ścieżce krytycznej po polsku (Kontynuuj/Nowa gra/Wznów/Zapisz/Postać/Imię/Panel debug).
8. Ekwipunek na dotyku — przyciski Weź/Wyrzuć wyraźnie łatwiejsze do trafienia niż wcześniej.

Nie commitowano żadnych zmian w tej sesji (zgodnie z poleceniem). 