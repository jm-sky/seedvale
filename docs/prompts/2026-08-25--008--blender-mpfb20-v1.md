You are working on the Seedvale project.

Repository:
\\wsl.localhost\Ubuntu-20.04\home\MY_USERNAME\projects\private\seedvale

Blender is running on Windows:
- Blender 5.2
- MPFB2 2.x
- Blender MCP provides Blender control through bpy.

IMPORTANT:
Before doing anything in Blender, read the Seedvale Blender/MPFB2 knowledge base:

\\wsl.localhost\Ubuntu-20.04\home\MY_USERNAME\projects\private\seedvale\docs\blender\README.md
\\wsl.localhost\Ubuntu-20.04\home\MY_USERNAME\projects\private\seedvale\docs\blender\AI_QUICK_REFERENCE.md
\\wsl.localhost\Ubuntu-20.04\home\MY_USERNAME\projects\private\seedvale\docs\blender\AI_WORKFLOW.md
\\wsl.localhost\Ubuntu-20.04\home\MY_USERNAME\projects\private\seedvale\docs\blender\SEEDVALE_CHARACTER_RULES.md
\\wsl.localhost\Ubuntu-20.04\home\MY_USERNAME\projects\private\seedvale\docs\blender\MPFB2_REFERENCE.md
\\wsl.localhost\Ubuntu-20.04\home\MY_USERNAME\projects\private\seedvale\docs\blender\MPFB2_RECIPES.md
\\wsl.localhost\Ubuntu-20.04\home\MY_USERNAME\projects\private\seedvale\docs\blender\BLENDER_5_2_REFERENCE.md

Also inspect relevant files under:

\\wsl.localhost\Ubuntu-20.04\home\MY_USERNAME\projects\private\seedvale\docs\blender\VERIFIED

Treat VERIFIED procedures as higher-confidence than unverified recipes.

Do not assume that a Blender/MPFB2 operation works just because it appears in a tutorial or generic knowledge. Prefer the installed Blender 5.2 behaviour, installed MPFB2 source/API, and the project documentation.

If the documentation conflicts with the actual installed MPFB2/Blender behaviour:
1. inspect the installed API/source,
2. determine the actual behaviour,
3. do not blindly follow outdated documentation,
4. report the discrepancy.

## Goal

Create the FIRST Seedvale male humanoid character prototype.

This is a proof-of-concept for the future Seedvale modular NPC character pipeline.

Do NOT generate multiple NPCs.
Do NOT build the complete automation pipeline yet.
Do NOT modify the Seedvale runtime code yet.

## Character

Create a realistic male medieval human:

- male
- approximately 35 years old
- average body
- approximately 178 cm
- realistic human proportions
- ordinary villager/adventurer appearance
- no exaggerated heroic proportions
- no cartoon/anime/stylized appearance

Appearance:

- natural face
- short/medium medieval-compatible male hairstyle
- medium beard
- natural brown hair
- natural skin tone

Use MPFB2 parameters rather than manually sculpting the character whenever possible.

## Clothing

Use the best currently available MPFB2-compatible clothing assets from the installed asset library.

Target:

- simple medieval tunic/shirt
- trousers
- boots
- optional simple belt
- no armor
- no fantasy accessories

Keep body, hair, beard and clothing logically modular where the MPFB2 asset system allows it.

Do not download random external assets unless the project documentation explicitly identifies them as approved.

## Rig

Prepare a humanoid rig suitable for the Seedvale animation pipeline.

Prefer the project's documented Mixamo-compatible workflow.

Do not invent a custom skeleton if the existing documented workflow provides the required result.

## Geometry / LOD

Target:

LOD0:
- approximately 20–30k triangles

LOD1:
- approximately 8–15k triangles

LOD2:
- approximately 3–5k triangles

Use the project's documented LOD/optimization procedure.

Do not blindly decimate everything. Preserve:
- silhouette
- face
- hands
- clothing readability
- deformation quality

Avoid unnecessary geometry duplication.

## Materials

Prepare materials so that runtime customization will be possible later where appropriate.

In particular, determine whether:
- skin colour can be changed at runtime,
- hair colour can be changed at runtime,
- clothing colours can be changed at runtime.

Do not create separate models for colour variants.

## Validation

Before export, validate:

- mesh integrity
- normals
- transforms
- armature
- skinning
- material assignments
- LOD hierarchy
- triangle counts
- missing textures
- unnecessary hidden geometry
- GLB compatibility

Use Blender 5.2 and the installed MPFB2 version as the source of truth.

## Export

Export a GLB suitable for the Seedvale Three.js/WebGL2 pipeline.

Also save the Blender source file.

Use a clear structure under:

docs/blender / appropriate project asset directory

Do not modify unrelated Seedvale files.

## Important architectural rule

This character is a prototype for a modular character system.

The final architecture should eventually allow:

Body
+ Hair
+ Beard
+ Clothing
+ Equipment

to be composed into many NPC variants.

Do NOT bake every combination into a separate character model.

The runtime should eventually be able to vary:
- sex
- age
- body shape
- height
- face
- hair
- hair colour
- beard
- clothing
- clothing colours
- equipment

For this task, only prove the technical feasibility with ONE male character.

## Working method

Work incrementally.

After each major step:
1. inspect the Blender result,
2. verify the actual state,
3. continue only if the result is correct.

Do not hide failures.

If an operation fails:
- inspect the error,
- consult TROUBLESHOOTING.md,
- inspect installed APIs/source if necessary,
- fix the root cause,
- record important newly discovered behaviour.

Do not replace a failed operation with an unrelated workaround without explaining why.

## Final report

At the end report:

1. Blender version
2. MPFB2 version
3. character parameters used
4. hair asset used
5. beard asset used
6. clothing assets used
7. triangle count for LOD0/LOD1/LOD2
8. number of meshes/materials
9. armature/skeleton used
10. whether Mixamo compatibility was confirmed
11. whether skin/hair/clothing colours can be changed independently
12. whether hair/clothing are separate modular meshes
13. GLB export path
14. Blender source path
15. any problems or documentation discrepancies discovered

Do not claim something is verified unless it was actually tested in Blender.

If this prototype succeeds, the next task will be designing the reusable Seedvale character asset pipeline. Do not implement that next task now.
