You are working on the Seedvale project.

Repository:
\\wsl.localhost\Ubuntu-20.04\home\madeyskij\projects\private\seedvale

Blender is running on Windows:
- Blender 5.2
- MPFB2 2.x
- Blender MCP provides Blender control through bpy.

IMPORTANT:
Before doing anything in Blender, read the Seedvale Blender/MPFB2 knowledge base:

\\wsl.localhost\Ubuntu-20.04\home\madeyskij\projects\private\seedvale\docs\blender\README.md
\\wsl.localhost\Ubuntu-20.04\home\madeyskij\projects\private\seedvale\docs\blender\AI_QUICK_REFERENCE.md
\\wsl.localhost\Ubuntu-20.04\home\madeyskij\projects\private\seedvale\docs\blender\AI_WORKFLOW.md
\\wsl.localhost\Ubuntu-20.04\home\madeyskij\projects\private\seedvale\docs\blender\SEEDVALE_CHARACTER_RULES.md
\\wsl.localhost\Ubuntu-20.04\home\madeyskij\projects\private\seedvale\docs\blender\MPFB2_REFERENCE.md
\\wsl.localhost\Ubuntu-20.04\home\madeyskij\projects\private\seedvale\docs\blender\MPFB2_RECIPES.md
\\wsl.localhost\Ubuntu-20.04\home\madeyskij\projects\private\seedvale\docs\blender\BLENDER_5_2_REFERENCE.md

Also inspect relevant files under:

\\wsl.localhost\Ubuntu-20.04\home\madeyskij\projects\private\seedvale\docs\blender\VERIFIED

Treat VERIFIED procedures as higher-confidence than unverified recipes.

Do not assume that a Blender/MPFB2 operation works just because it appears in a tutorial or generic knowledge.

Prefer, in this order:
1. actual behaviour of the running Blender 5.2 installation,
2. actual installed MPFB2 source/API,
3. project documentation,
4. generic Blender/MPFB2 knowledge.

If the documentation conflicts with the actual installed MPFB2/Blender behaviour:
1. inspect the installed API/source,
2. determine the actual behaviour,
3. do not blindly follow outdated documentation,
4. report the discrepancy.

Do not download random external assets.
Do not install additional software or Blender addons unless explicitly necessary and approved.
Do not modify unrelated Seedvale files.

---

# PHASE 0 — DISCOVERY FIRST

Before creating or modifying any character, establish what is actually available in the running Blender installation.

Use Blender MCP to inspect the actual Blender session.

Confirm:

1. Blender is reachable through MCP.
2. Blender version.
3. MPFB2 version.
4. MPFB2 is operational.
5. Location of the installed MPFB2 addon/source/API.
6. Location of MPFB2 asset libraries.
7. Available human/base-character assets.
8. Available male/female body options.
9. Available hair assets.
10. Available beard/facial-hair assets.
11. Available clothing assets.
12. Available medieval or historically appropriate clothing assets.
13. Available footwear assets.
14. Available accessories if relevant.
15. Available rig/skeleton options.
16. Existing documented Mixamo-compatible workflow.
17. Whether the required assets can actually be loaded in the current Blender session.

Inspect the filesystem and installed MPFB2 source when necessary.

Do not rely on filenames alone.
If possible, verify important assets by actually loading or inspecting them through Blender.

Do NOT create the final character yet.

First produce a concise internal discovery report and use the findings to select the assets for the prototype.

If required assets are missing, inaccessible, incompatible or cannot be loaded:
- investigate the actual reason,
- inspect the installed API/source if useful,
- consult relevant troubleshooting documentation,
- do not invent assets,
- do not silently substitute unrelated external assets.

If the prototype is still technically feasible with the installed assets, continue.

---

# GOAL

Create the FIRST Seedvale male humanoid character prototype.

This is a proof-of-concept for the future Seedvale modular NPC character pipeline.

Do NOT generate multiple NPCs.

Do NOT build the complete automation pipeline yet.

Do NOT modify the Seedvale runtime code yet.

Do NOT perform unrelated repository cleanup or refactoring.

The purpose of this task is to prove that we can reliably create a high-quality modular human character using the actual installed Blender + MPFB2 environment and export it for Seedvale.

---

# CHARACTER

Create a realistic male medieval human:

- male
- approximately 35 years old
- average body
- approximately 178 cm
- realistic human proportions
- ordinary villager/adventurer appearance
- no exaggerated heroic proportions
- no cartoon/anime/stylized appearance

Appearance:

- natural face
- short/medium medieval-compatible male hairstyle
- medium beard
- natural brown hair
- natural skin tone

Use MPFB2 parameters rather than manually sculpting the character whenever possible.

Do not manually remodel the character unless required to solve a specific technical problem.

The character should look like an ordinary inhabitant of Seedvale rather than a fantasy hero.

---

# CLOTHING

Use the best suitable MPFB2-compatible clothing assets that are actually available in the installed asset library.

Select assets based on actual availability and compatibility in the current Blender/MPFB2 installation.

Target:

- simple medieval tunic/shirt
- trousers
- boots
- optional simple belt
- no armor
- no fantasy accessories

Prefer historically/visually coherent pieces.

Keep these components logically modular where the MPFB2 asset system allows it:

- body
- hair
- beard
- shirt/tunic
- trousers
- boots
- belt/accessories

Do not bake every possible combination into a single character mesh.

Do not download random external assets.

---

# MODULARITY

This prototype is specifically intended to validate a future modular Seedvale NPC character system.

The desired conceptual structure is:

Body
+ Hair
+ Beard
+ Clothing
+ Equipment

→ NPC variant

The eventual runtime should be able to vary:

- sex
- age
- body shape
- height
- face
- hair
- hair colour
- beard
- clothing
- clothing colours
- equipment

For this task, only prove technical feasibility with ONE male character.

Where practical, keep hair, beard and clothing as separate meshes rather than permanently joining everything into one mesh.

Do not sacrifice working deformation or export quality merely to preserve theoretical modularity.

Document any MPFB2 limitations that affect modularity.

---

# RIG

Prepare a humanoid rig suitable for the Seedvale animation pipeline.

Prefer the project's documented Mixamo-compatible workflow.

Do not invent a custom skeleton if the existing documented workflow provides the required result.

Verify the actual skeleton and skinning rather than assuming compatibility from its name.

If Mixamo compatibility cannot be directly confirmed in the local environment, report exactly what was and was not verified.

Do not claim Mixamo compatibility merely because the skeleton appears humanoid.

---

# GEOMETRY / LOD

Triangle counts are targets, not hard requirements.

Target approximately:

LOD0:
- 20–30k triangles

LOD1:
- 8–15k triangles

LOD2:
- 3–5k triangles

Use the project's documented LOD/optimization procedure.

Prioritize, in this order:

1. successful character generation,
2. clean modular structure,
3. good silhouette,
4. good face readability,
5. good hands,
6. good clothing readability,
7. deformation quality,
8. sensible triangle counts.

Do not blindly decimate everything.

Preserve important geometry around:

- face
- hands
- joints
- silhouette
- clothing edges
- deformation areas

Avoid unnecessary geometry duplication.

If the initial character exceeds the target significantly, investigate the source of the geometry before applying destructive optimization.

---

# MATERIALS

Prepare materials so that runtime customization will be possible later where appropriate.

Determine whether the current character setup allows independent runtime variation of:

- skin colour
- hair colour
- clothing colours

Do not create separate models for colour variants.

Determine whether these variations can be implemented by changing material parameters/textures rather than duplicating geometry.

If MPFB2's material setup prevents clean runtime customization, document the limitation and identify the actual technical reason.

Do not modify Seedvale runtime code for this task.

---

# BLENDER STRUCTURE

Keep the Blender file clean and understandable.

Use clear names for:

- character
- body
- hair
- beard
- clothing
- footwear
- armature
- LOD objects

Avoid unnecessary duplicate meshes, collections and materials.

Do not leave failed experimental objects in the final file unless they are clearly isolated and useful for debugging.

Do not destroy useful source information merely to simplify the final scene.

---

# VALIDATION

Before export, validate the actual result.

Check:

- mesh integrity
- normals
- transforms
- scale
- armature
- skinning
- deformation
- material assignments
- texture references
- LOD hierarchy
- triangle counts
- missing textures
- unnecessary hidden geometry
- duplicate geometry
- duplicate materials
- object naming
- GLB compatibility

Where possible, inspect the character in Blender after all major operations.

Do not claim validation merely because an operation completed without throwing an exception.

---

# GLB EXPORT

Export a GLB suitable for the Seedvale Three.js/WebGL2 pipeline.

The exported asset should preserve:

- geometry
- materials
- textures
- armature
- skinning
- modular meshes where appropriate
- LOD structure if supported by the project's export/runtime conventions

Use the project's documented GLB export procedure.

Do not invent a new export pipeline unless the documented one is incompatible with the actual Blender 5.2 environment.

Also save the Blender source file.

Use a clear structure under the appropriate project asset directory.

Do not modify unrelated Seedvale files.

---

# FILE OUTPUT

Before deciding where to save the files, inspect the repository documentation for the intended Blender/character asset locations.

Do not assume a path if the project documentation already defines one.

Produce:

1. Blender source `.blend`
2. final `.glb`

Use clear, descriptive filenames.

If additional intermediate files are genuinely required, keep them organized and explain their purpose.

---

# WORKING METHOD

Work incrementally.

After each major step:

1. inspect the Blender result,
2. verify the actual state,
3. compare it with the expected result,
4. continue only if the result is correct.

Major steps include:

1. discovery,
2. base human generation,
3. body parameters,
4. hair,
5. beard,
6. clothing,
7. rig,
8. materials,
9. LOD,
10. validation,
11. GLB export.

Do not blindly chain many Blender operations without checking intermediate state.

Do not hide failures.

If an operation fails:

1. inspect the exact error,
2. consult relevant `VERIFIED` documentation,
3. consult `TROUBLESHOOTING.md` if applicable,
4. inspect installed MPFB2 source/API if necessary,
5. determine the root cause,
6. retry using the correct actual API/workflow.

Do not replace a failed operation with an unrelated workaround without explaining why.

If a workaround is necessary, prefer one that preserves the intended Seedvale architecture.

---

# FAILURE HANDLING

If MPFB2 cannot perform an operation as expected:

Do not immediately abandon MPFB2.

First determine whether the problem is:

- wrong API assumption,
- outdated documentation,
- Blender 5.2 API difference,
- MPFB2 version difference,
- missing asset library,
- incorrect asset path,
- missing dependency,
- incorrect operator/context,
- incompatible asset,
- MCP limitation,
- or a genuine MPFB2 limitation.

Use the installed source/API as the source of truth when documentation is insufficient.

If a genuine blocker remains, stop at the smallest reasonable point and report it instead of creating a poor substitute character.

---

# IMPORTANT ARCHITECTURAL RULES

This character is a prototype for a modular character system.

Do not create a separate baked character for every profession.

Future NPCs should ideally reuse the same underlying character system.

For example:

Farmer:
Body + Hair + Beard + Farmer Clothing + Tool

Guard:
Body + Hair + Beard + Guard Clothing + Weapon

Hunter:
Body + Hair + Beard + Hunter Clothing + Bow

Merchant:
Body + Hair + Beard + Merchant Clothing + Accessories

The profession should primarily be represented through clothing, equipment and other modular components rather than requiring an entirely new humanoid model.

Do not implement this system now.

Only make the first prototype compatible with this future direction.

---

# PERFORMANCE / SEEDVALE CONSIDERATIONS

Seedvale may contain many NPCs simultaneously.

Do not optimize prematurely, but avoid obviously wasteful asset construction.

Avoid:

- unnecessary high-resolution geometry,
- duplicated textures,
- duplicated materials,
- permanently duplicated body meshes,
- unnecessary hidden geometry,
- unnecessary modifiers in the exported asset.

The final character should be suitable as a basis for future NPC instancing and modular composition.

---

# DOCUMENTATION DISCOVERY

If you discover important behaviour that is not correctly documented, record it for the final report.

Especially note:

- MPFB2 API differences,
- Blender 5.2 differences,
- asset library locations,
- asset loading requirements,
- rigging requirements,
- clothing attachment behaviour,
- material behaviour,
- export limitations,
- MCP limitations.

Do not modify project documentation during this prototype unless explicitly necessary.

The purpose of this task is to discover the actual workflow first.

---

# FINAL REPORT

At the end, report:

1. Blender version
2. MPFB2 version
3. MPFB2 installation/API location
4. asset library location(s)
5. character parameters used
6. hair asset used
7. beard asset used
8. clothing assets used
9. footwear asset used
10. triangle count for LOD0
11. triangle count for LOD1
12. triangle count for LOD2
13. number of meshes
14. number of materials
15. armature/skeleton used
16. whether Mixamo compatibility was confirmed
17. whether skin colour can be changed independently
18. whether hair colour can be changed independently
19. whether clothing colours can be changed independently
20. whether hair is a separate modular mesh
21. whether beard is a separate modular mesh
22. whether clothing is represented by separate modular meshes
23. GLB export path
24. Blender source path
25. important problems encountered
26. documentation discrepancies discovered
27. MPFB2/Blender/MCP limitations discovered
28. whether the prototype successfully proves the intended character pipeline

For every "verified" claim, describe briefly what was actually tested.

Do not claim something is verified merely because it appears theoretically possible.

---

# STOP CONDITION

Do not continue beyond the first prototype.

Do not implement:

- the reusable character generation pipeline,
- automated NPC generation,
- profession variants,
- runtime character customization,
- Seedvale runtime integration,
- additional characters,
- batch generation,
- procedural clothing systems.

If this prototype succeeds, stop and report the results.

The next task will be designing the reusable Seedvale character asset pipeline.

Do not implement that next task now.
